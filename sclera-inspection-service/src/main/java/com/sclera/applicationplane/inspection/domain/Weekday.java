package com.sclera.applicationplane.inspection.domain;

/** Schedule days (Mon–Sun). All selected by default on the config form. */
public enum Weekday {
    MON,
    TUE,
    WED,
    THU,
    FRI,
    SAT,
    SUN
}
